#include <iostream>
#include <vector>

using namespace std;

vector<int> coins = {1, 5, 10, 25};

int numCombs(int sum, int bI) {
	if (sum <= 0) return 0;
	}
	
	return sums;
}

int main() {
	auto temp = coins(25);
	std::cout << temp;
}
